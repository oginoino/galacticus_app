class CheckinFilterOption {
  const CheckinFilterOption({
    required this.label,
    required this.icon,
    required this.isSelected,
  });

  final String label;
  final String icon;
  final bool isSelected;
}
