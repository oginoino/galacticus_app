class CalendarDay {
  const CalendarDay({
    required this.label,
    required this.isSelected,
    required this.isActive,
  });

  final String label;
  final bool isSelected;
  final bool isActive;
}
